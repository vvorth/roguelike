Task: T13
Status: complete
Files created/modified:
- roguelike/tiles.py (amended) — added STAIRS_UP=3, STAIRS_DOWN=4 to Tile; extended TILE_CHARS
  and WALKABLE; added new STAIRS frozenset; updated docstrings.
- roguelike/level.py (amended) — appended stairs_up, stairs_down, depth fields to Level with
  defaults; added the three __post_init__ ValueError checks.
- tests/test_level.py (extended) — extended the two table-equality assertions
  (test_tile_members_and_values, test_tile_chars_mapping, test_walkable_set) to include the new
  tiles; added test_stairs_set, test_is_walkable_tile parametrize cases for both stairs; added a
  new "Level stairs and depth" section (test_level_no_stairs_by_default,
  test_level_rejects_non_positive_depth, test_level_accepts_depth_one/greater_than_one,
  test_level_stairs_up_none_is_accepted, test_level_rejects_out_of_bounds_stairs_up,
  test_level_accepts_in_bounds_stairs_up, test_level_stairs_down_empty_tuple_is_accepted,
  test_level_rejects_out_of_bounds_stairs_down_entry(_among_valid_entries),
  test_level_accepts_two_entry_stairs_down_both_in_bounds,
  test_level_stairs_down_is_a_tuple_not_a_scalar, test_level_does_not_require_stairs_to_exist,
  test_level_stairs_and_depth_are_frozen, test_level_grid_containing_stairs_reports_them_and_is_walkable);
  also test_level_field_order_via_dataclasses_fields and
  test_level_positional_construction_with_all_nine_fields, and extended
  test_level_positional_field_order to assert the three new defaults.
- .plan/reports/T13.md (this report)

Public surface (symbols + signatures actually exported):
roguelike/tiles.py:
  class Tile(IntEnum): WALL=0, FLOOR=1, DOOR=2, STAIRS_UP=3, STAIRS_DOWN=4
  TILE_CHARS: dict[Tile, str]  # + STAIRS_UP:"<", STAIRS_DOWN:">"
  WALKABLE: frozenset[Tile]    # {FLOOR, DOOR, STAIRS_UP, STAIRS_DOWN}
  STAIRS: frozenset[Tile]      # {STAIRS_UP, STAIRS_DOWN}  (new)
  PLAYER_CHAR: str = "@"
  DOOR_OPEN_CHAR: str = "'"
  def tile_char(tile: Tile) -> str
  def is_walkable_tile(tile: Tile) -> bool
  __all__ updated to include "STAIRS"

roguelike/level.py:
  @dataclass(frozen=True)
  class Level:
      width: int
      height: int
      grid: tuple[tuple[Tile, ...], ...]
      rooms: tuple[Room, ...]
      player_start: tuple[int, int]
      seed: int
      stairs_up: tuple[int, int] | None = None
      stairs_down: tuple[tuple[int, int], ...] = ()
      depth: int = 1
  (Room, freeze_grid, blank_grid unchanged; __all__ unchanged.)

Contract deviations: none.

Assumptions made:
- Interpreted "in bounds" for stairs_up/stairs_down entries as Level.in_bounds(x, y), matching
  the existing player_start check and CONTRACT-v3 §2's wording.
- Added a handful of tests beyond the literal acceptance-criteria list (e.g.
  test_level_stairs_down_is_a_tuple_not_a_scalar, test_level_does_not_require_stairs_to_exist)
  to pin down behaviour explicitly called out as load-bearing in the brief; these are ordinary
  extensions of the required test coverage, not new production surface.
- Did not touch world.py, style.py, fov.py, movement.py, generator.py, render.py, game.py,
  keys.py, events.py, dungeon.py, main.py, or any other tests/ file, per the brief's "do not
  touch" list. Did not add a Tile.STAIRS member, an is_stairs_tile() helper, or any
  stair-placement/descent logic.

Verification: `cd /Users/vvorth/Documents/claude-home && .venv/bin/python -m pytest tests/test_level.py -q < /dev/null` → exit 0, 117 passed (85 pre-existing + 32 new), 0 failed.

Open questions for integrator: none.
